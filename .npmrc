# 根工程同时把 tdesign-icons-vue (Vue 2) 与 tdesign-icons-vue-next (Vue 3)
# 作为 devDependencies 用于单测/示例验证，二者 peer 依赖的 vue 版本冲突。
# npm 用 legacy-peer-deps 忽略冲突；pnpm 下对应 strict-peer-dependencies=false
#（默认即为 false，仅告警不阻断），这里显式声明以明确意图。
# 消费方安装本包不受影响，peer 均为 optional。
strict-peer-dependencies=false
