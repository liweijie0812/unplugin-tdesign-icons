# pnpm 11 配置
# allowBuilds：允许指定依赖执行安装脚本（v11 取代了旧版 onlyBuiltDependencies）。
# esbuild 等依赖需要在安装时构建原生二进制，必须显式允许。
allowBuilds:
  esbuild: true

# workspace：examples 用 `workspace:*` 引用根包，运行前需先构建根包。
packages:
  - 'examples/*'
